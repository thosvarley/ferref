/target

# The library database is user data, not source. Matches ./ferref.db created by
# running the CLI, and the stale src/ferref.db left over from early development.
ferref.db

# PDFs downloaded by `ferref fetch`. User data like the database, and large.
pdfs/

restart_claude.sh

.claude/
